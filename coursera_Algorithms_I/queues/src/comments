/**
     * @InheritDoc
     */
    @Override
    public String toString() {
        String str = "";
        for (int i = 0; i < list.length; i++) {
            if (list[i] == null) str += "-";
            else str += "x";
        }
        return str;
    }